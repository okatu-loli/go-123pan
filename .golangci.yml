version: "2"

linters:
  settings:
    errcheck:
      # defer xx.Close() 是 Go 惯用写法，只读场景无需检查返回值
      exclude-functions:
        - (io.Closer).Close
        - (io.ReadCloser).Close
        - (*os.File).Close
